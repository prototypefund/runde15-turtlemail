{% extends "turtlemail/layout.jinja" %}
{% block content %}
    <div class="flex flex-col items-center justify-center gap-8">
        <div class="p-4 bg-base-100 rounded-box">
            <a href="{{ url("create_packet") }}" class="btn btn-primary">{{ _("Place a delivery") }}</a>
        </div>
    </div>
{% endblock %}
